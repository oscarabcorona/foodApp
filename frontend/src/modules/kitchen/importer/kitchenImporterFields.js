import model from 'modules/kitchen/kitchenModel';

const { fields } = model;

export default [
  fields.category,
  fields.name,
  fields.employee,
  fields.quantity,
  fields.observation,
  fields.status,
];
