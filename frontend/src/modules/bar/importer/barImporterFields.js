import model from 'modules/bar/barModel';

const { fields } = model;

export default [
  fields.category,
  fields.name,
  fields.employee,
  fields.quantity,
  fields.status,
  fields.observation,
];
