import model from 'modules/restaurant/restaurantModel';

const { fields } = model;

export default [
  fields.name,
  fields.employee,
  fields.products,
  fields.country,
  fields.city,
  fields.phoneNumber,
];
