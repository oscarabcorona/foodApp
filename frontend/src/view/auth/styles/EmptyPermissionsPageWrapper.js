import { styled } from '@material-ui/core/styles';
import Wrapper from 'view/auth/styles/Wrapper';

const ForgotPasswordPageWrapper = styled(Wrapper)({
  backgroundImage: 'url(/images/emptyPermissions.jpg)',
});

export default ForgotPasswordPageWrapper;
